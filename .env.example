# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=kehuxinxi
DB_USER=root
DB_PASSWORD=your_password

# JWT配置（安全要求：必须设置强密钥）
# 生成随机密钥命令：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_very_strong_random_secret_key_here_at_least_64_characters_long
JWT_EXPIRES_IN=7d

# 服务端口
PORT=9527

# 邮件配置（用于跟进提醒）
SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_USER=your_email@qq.com
SMTP_PASS=your_email_password
